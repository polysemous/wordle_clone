rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser never talks to Firestore. Only the Cloud Run service account
    // accesses data through the Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
